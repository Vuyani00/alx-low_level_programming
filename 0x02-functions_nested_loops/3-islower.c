#include "main.h"
#include <ctype.h>

/**
  *_islower - checks for lowercase letters
  *
  *Descrption: checks whether a character is lowercase/uppercase
  *
  *Return: 0 (Success)
  */

int _islower(int c)
{
	char alpha;
	/*c - integer*/
	int c = 0;

	if (islower(alpha) > c)
	{
		return (1);
	} else if (alpha == c)
	{
		return (0);
	}
}
